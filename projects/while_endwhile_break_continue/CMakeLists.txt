cmake_minimum_required(VERSION 3.2.0)
project(while)
set(counter 0)
while(counter LESS 10)
	math(EXPR result ${counter}%2)
	if(result EQUAL 0 AND counter GREATER 0)
		message("счетчик четный")
	endif()
math(EXPR counter ${counter}+1)
message("counter: ${counter}")
endwhile()
