cmake_minimum_required(VERSION 3.2.0)
project(find_file)
set(CMAKE_INCLUDE_PATH "${PROJECT_SOURCE_DIR}/dir1")
find_path(FILE_PATH "file.txt")
if(EXISTS ${FILE_PATH})
	message("file.txt find in: ${FILE_PATH}")
else() 
	message("something went wrong...")
endif()
